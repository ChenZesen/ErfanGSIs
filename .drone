#!/usr/bin/env bash

# Kernel CI Builder
# Copyright (C) chen
# Default Settings
export ARCH=arm64
export SUBARCH=arm64
export HOME=/drone

df -h

free -m
apt-get update
apt-get install -y neofetch screenfetch

neofetch

screenfetch
